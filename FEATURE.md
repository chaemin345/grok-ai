# 제품 원칙 & 추천 기능

## 핵심 원칙
1. **빨간 밑줄 + 각주**가 제품의 중심이다. AI가 문장을 고쳐 쓰지 않는다.
2. **단순 법 대조**(법령명·조 인용 존재 여부)는 컴퓨터(규칙 + 국가법령 API)가 한다.
3. **문맥·논리 이상**만 Claude Pro 등 LLM에 맡긴다. (`lib/context-check.ts`)

## 문맥교정 (사업성 레이어)
- 전제와 결론이 안 맞는 부분
- 조문을 인용했지만 논증 흐름이 어긋난 부분
- 서로 모순되는 주장

이 구간에도 동일하게 **밑줄 + 각주(이유)** 만 제공한다. 자동 수정은 하지 않는다.

환경변수 `CLAUDE_API_KEY` 설정 후 `enableContextCheck: true` 로 호출하면 활성화 지점이다.

## 다음 유료 기능 후보
**PDF 검증 리포트 다운로드** — 밑줄·각주·점수가 들어간 보관용 PDF. B2B에 가장 잘 팔린다.
